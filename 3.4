sum_of_digits <- function(num){
  sum <- 0
  while(num>0){
    digit <- num%%10
    sum <- sum + digit
    num <- num%/%10
  }
  return(sum)
  
    
  }
num <- as.numeric(readline("enter the number"))
res <- sum_of_digits(num)
print(paste("sum of digits using while loop",res))
